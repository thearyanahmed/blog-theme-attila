{{!< default}}
<header class="blog-header {{#if @site.cover_image}} has-cover{{/if}}">
	<div class="inner">
		<div class="box blog-box">

            <div class="pt-20 flex flex-row justify-center">
                <img src="https://avatars.githubusercontent.com/u/20617352?v=4"
                     class="w-20 h-20 rounded-full" alt="">
            </div>

			{{#if @site.title}}
			    <span class="text-4xl md:text-6xl cursor-pointer home-page-blog-title blog-description">{{@site.title}}</span>
			{{/if}}

			{{#if @site.description}}
			    <span class="text-3xl md:text-3xl blog-description">{{@site.description}}</span>
			{{/if}}

            <div class="mt-8 md:mt-10 text-2xl text-gray-500 leading-normal secondary-font">
                <p>
                    I'm a programmer. Currently working at <a class="text-blue-600" href="https://www.radicalel.com/" target="_blank">Radical Elements</a> as a backend dev.
                </p>

                <p class="mt-2">
                    I work on the PHP stack,GO, python's fastapi mostly for backend. Besides these, I tinker with various tech like docker, terraform, aws etc.
                </p>

                <p class="mt-2">Currently maintaining, <a href="https://skridi.com/" class="text-blue-600" target="_blank">Skridi</a> & <a href="#" class="text-blue-600"  target="_blank">Matx</a></p>
            </div>

            {{#is "home"}}
                <div class="secondary-nav-header mt-10 text-gray-600">
                    <div class="nav-wrapper text-xl md:text-2xl " aria-label="{{t "Main"}}">
                        {{navigation}}
                    </div>
                </div>
            {{/is}}


        </div>
		{{#if @site.cover_image}}
		<div class="blog-cover cover">
			<img
			srcset="{{img_url @site.cover_image size="s"}} 320w,
              {{img_url @site.cover_image size="m"}} 640w,
              {{img_url @site.cover_image size="l"}} 960w,
              {{img_url @site.cover_image size="xl"}} 1920w"
      src="{{img_url @site.cover_image size="xl"}}"
			alt="{{@site.title}}" />
		</div>
		{{/if}}
	</div>
</header>

<div id="index" class="container">

	<main class="content" role="main">

		{{> "loop"}}

	</main>

</div>
